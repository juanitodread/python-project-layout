# python-project-layout
This version is hardcoded to my Python directory installation. You can modify accordingly. Also assumes you have `mkvirtualenv` installed in your machine's Python environment.

This script creates a new Python project structure, generates a `virtualenv` and prepare it for git

I

## How to use it
1. Run init.sh `$ sh init.sh <your-repo-name>`