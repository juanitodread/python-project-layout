from setuptools import setup


setup(
   name='',
   version='',
   description='',
   author='',
   author_email='',
   packages=[''],  #same as name
   install_requires=[], #external packages as dependencies
   scripts=[]
)
